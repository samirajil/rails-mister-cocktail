<div class="form-box">
  <div class="d-flex flex-column align-items-center">
    <h1 class="mb-4"><%= @cocktail.name %></h1>
    <%= cl_image_tag(@cocktail.photo.key, height: 100, width: 100, crop: :fill) %>
    <h3 class="mt-5">Ingredients:</h3>
    <ul>
      <% @cocktail.doses.each do |dose| %>
        <li><%= dose.description %> <%= dose.ingredient.name %><%= link_to "  🗑", dose_path(dose.id), method: :delete, data: {confirm: "Are you sure?"}%></li>
      <% end %>
      <%= render 'doses/form' %>
    </ul>
  </div>
</div>
